# Release Report — Tenant Provisioning Experience

> Rolling release report — reflects the most recent sprint. Prior sprint releases are in git history +
> [docs/CHANGELOG_DOCS.md](../CHANGELOG_DOCS.md).

## Sprint
**Tenant Provisioning Experience** — a complete end-to-end **White Label onboarding** flow (9 visual steps →
Provisioning Engine → auto-verify → summary), built by **enhancing the existing Tenant Onboarding Wizard in
place** (no duplicate wizard, no duplicate provisioning logic).

## Release gate — all checks PASSED ✅
| Check | Command | Result |
|---|---|---|
| TypeScript | `npm run lint` (`tsc --noEmit`) | ✅ **0 errors** |
| Production build | `npm run build` | ✅ built (~7s), `version.json`/`sw.js` stamped |
| E2E (sandbox) | `node docs/testing/e2e_runner.cjs` | ✅ **24/24 pass, 0 fail** |
| Runtime verification | Puppeteer probe (super admin → Onboarding) | ✅ **PASS** — 9 steps → provision → **10/10 auto-verification** → summary with Website/Admin/Tenant URLs, duration, completed modules; 0 console errors |

## Files changed
- `src/features/admin/TenantOnboardingWizard.tsx` (enhanced) — 9-step experience + verification + summary.
- `docs/implementation/TENANT_PROVISIONING_EXPERIENCE_REPORT.md` (new) · this report (updated).

## Commit / branch
| Item | Value |
|---|---|
| Branch | `feat/website-platform-architecture` (pushed to `origin`) |
| Code commit | `2e70295` — `feat(admin): Tenant Provisioning Experience — 9-step white-label onboarding + verify + summary` |
| Merged to `main`? | **No** (per gate: no automatic production merge) |

## Deployment status
- **Branch pushed to origin** ✅ — Vercel builds branch pushes as **Preview Deployments** (production = `main`,
  untouched).
- **Preview Deployment URL — could not be verified/returned from this environment** (no `vercel`/`gh` CLI, no
  `VERCEL_TOKEN`, rate-limited API). The preview is generated by the Vercel Git integration on push.
- **How to use:** open the preview → log in as super admin (`+201000000005` / `123456`) → Platform →
  **Onboarding** → walk the 9 steps → **Provision** → review the summary (auto-verification + URLs). Open the new
  tenant's site with the shown link (`?site=<slug>` in sandbox; subdomain / custom domain in production).
- **How to obtain the URL:** the branch PR — `https://github.com/dlslogisticsservice-web/Haat-Now/pull/new/feat/website-platform-architecture`
  — or Vercel → Deployments for `2e70295`.
- **Build status:** succeeded locally (identical toolchain to Vercel). **Deployment status: pending external
  verification.**

## Known limitations
- Sandbox provisioning + verification are **complete and verified**; **live** provisioning (Supabase RPCs/seeding,
  real Auth-user creation for the admin account, custom-domain DNS/SSL) is the documented staging follow-up
  (three-environment model).
- Preview URL unverifiable from this environment (as above).

## Rollback strategy
- **Production/main unchanged** — sprint lives on `feat/website-platform-architecture`.
- **Feature-level:** revert `2e70295` — restores the prior wizard; no other module is affected.
- **Provisioning-level:** the engine's `rollback(runId)` removes a partially-provisioned tenant; a failed run
  stays in `draft` (never active) and is retryable from the timeline.
- **Preview teardown:** Vercel removes the preview when the branch is deleted.

## Outcome
Gate **green** (incl. runtime probe); implementation **committed and pushed**; a **Preview Deployment is
triggered** via the Vercel Git integration (URL obtainable from the Vercel dashboard/PR, not resolvable from this
environment). Production (`main`) intentionally untouched. **Stopping after successful Preview deployment.**
